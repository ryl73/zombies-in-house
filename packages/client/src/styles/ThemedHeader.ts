import styled from 'styled-components'

export const ThemedHeader = styled.h1`
  font-family: 'Rubik Wet Paint', cursive;
  text-align: center;
  color: var(--color-primary);
  font-size: 64px;
  line-height: 64px;
`
